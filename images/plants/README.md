# Plant photos

Drop plant photos in this folder, then reference them from `plants.js` as
`photo: "images/plants/<filename>"`.

Naming: lowercase with hyphens, matching the plant — e.g. `butterfly-bush.jpg`.
